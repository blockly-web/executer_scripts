#!/bin/bash
set -euo pipefail

# Configuration: use a "temp" directory in the current working directory.
PORT=8080
TEMP_DIR="/var/lib/terraform-executer/temp"
mkdir -p "$TEMP_DIR"
export TEMP_DIR
echo "Using temporary directory: $TEMP_DIR" >&2

# ... rest of your script content ... 